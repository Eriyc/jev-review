{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "jev-review": {
      "type": "stdio",
      "command": "bun",
      "args": ["${PLUGIN_ROOT}/dist/server.js"],
      "cwd": "./"
    }
  }
}
